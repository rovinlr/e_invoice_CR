<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_hacienda_cabys_tree" model="ir.ui.view">
        <field name="name">hacienda.cabys.list</field>
        <field name="model">hacienda.cabys</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Códigos CABYS" create="false" edit="false">
                <field name="code"/>
                <field name="name"/>
                <field name="tax_rate"/>
                <field name="active" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_hacienda_cabys_form" model="ir.ui.view">
        <field name="name">hacienda.cabys.form</field>
        <field name="model">hacienda.cabys</field>
        <field name="arch" type="xml">
            <form string="Código CABYS">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="tax_rate"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hacienda_cabys_search" model="ir.ui.view">
        <field name="name">hacienda.cabys.search</field>
        <field name="model">hacienda.cabys</field>
        <field name="arch" type="xml">
            <search string="Buscar Códigos CABYS">
                <field name="code" filter_domain="['|', ('code', 'ilike', self), ('name', 'ilike', self)]"/>
                <filter string="Activos" name="active" domain="[('active', '=', True)]"/>
                <filter string="Inactivos" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="view_hacienda_measurement_unit_tree" model="ir.ui.view">
        <field name="name">hacienda.measurement.unit.list</field>
        <field name="model">hacienda.measurement.unit</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Unidades Hacienda" create="false" edit="false">
                <field name="code"/>
                <field name="name"/>
                <field name="uom_id"/>
            </list>
        </field>
    </record>

    <record id="view_hacienda_measurement_unit_form" model="ir.ui.view">
        <field name="name">hacienda.measurement.unit.form</field>
        <field name="model">hacienda.measurement.unit</field>
        <field name="arch" type="xml">
            <form string="Unidad Hacienda">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="uom_id" options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hacienda_measurement_unit_search" model="ir.ui.view">
        <field name="name">hacienda.measurement.unit.search</field>
        <field name="model">hacienda.measurement.unit</field>
        <field name="arch" type="xml">
            <search string="Buscar Unidades Hacienda">
                <field name="code" filter_domain="['|', ('code', 'ilike', self), ('name', 'ilike', self)]"/>
            </search>
        </field>
    </record>

    <record id="view_hacienda_canton_tree" model="ir.ui.view">
        <field name="name">hacienda.canton.list</field>
        <field name="model">hacienda.canton</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Cantones" create="false" edit="false">
                <field name="code"/>
                <field name="name"/>
                <field name="province_id"/>
            </list>
        </field>
    </record>

    <record id="view_hacienda_canton_form" model="ir.ui.view">
        <field name="name">hacienda.canton.form</field>
        <field name="model">hacienda.canton</field>
        <field name="arch" type="xml">
            <form string="Cantón">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="province_id" options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hacienda_canton_search" model="ir.ui.view">
        <field name="name">hacienda.canton.search</field>
        <field name="model">hacienda.canton</field>
        <field name="arch" type="xml">
            <search string="Buscar Cantones">
                <field name="code" filter_domain="['|', ('code', 'ilike', self), ('name', 'ilike', self)]"/>
                <field name="province_id"/>
            </search>
        </field>
    </record>

    <record id="view_hacienda_district_tree" model="ir.ui.view">
        <field name="name">hacienda.district.list</field>
        <field name="model">hacienda.district</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Distritos" create="false" edit="false">
                <field name="code"/>
                <field name="name"/>
                <field name="canton_id"/>
            </list>
        </field>
    </record>

    <record id="view_hacienda_district_form" model="ir.ui.view">
        <field name="name">hacienda.district.form</field>
        <field name="model">hacienda.district</field>
        <field name="arch" type="xml">
            <form string="Distrito">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="canton_id" options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hacienda_district_search" model="ir.ui.view">
        <field name="name">hacienda.district.search</field>
        <field name="model">hacienda.district</field>
        <field name="arch" type="xml">
            <search string="Buscar Distritos">
                <field name="code" filter_domain="['|', ('code', 'ilike', self), ('name', 'ilike', self)]"/>
                <field name="canton_id"/>
            </search>
        </field>
    </record>

    <record id="view_hacienda_neighborhood_tree" model="ir.ui.view">
        <field name="name">hacienda.neighborhood.list</field>
        <field name="model">hacienda.neighborhood</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Barrios" create="false" edit="false">
                <field name="code"/>
                <field name="name"/>
                <field name="district_id"/>
            </list>
        </field>
    </record>

    <record id="view_hacienda_neighborhood_form" model="ir.ui.view">
        <field name="name">hacienda.neighborhood.form</field>
        <field name="model">hacienda.neighborhood</field>
        <field name="arch" type="xml">
            <form string="Barrio">
                <sheet>
                    <group>
                        <field name="code"/>
                        <field name="name"/>
                        <field name="district_id" options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hacienda_neighborhood_search" model="ir.ui.view">
        <field name="name">hacienda.neighborhood.search</field>
        <field name="model">hacienda.neighborhood</field>
        <field name="arch" type="xml">
            <search string="Buscar Barrios">
                <field name="code" filter_domain="['|', ('code', 'ilike', self), ('name', 'ilike', self)]"/>
                <field name="district_id"/>
            </search>
        </field>
    </record>
</odoo>
